[package]
name = "jellyhaj-form-derive-impl"
version = "0.1.0"
edition = "2024"

[dependencies]
proc-macro2 = {workspace = true}
quote = {workspace = true}
syn = {features = ["clone-impls", "derive",
"extra-traits", "full", "parsing", "printing"], workspace = true}

[dev-dependencies]
pretty_assertions = "1.4.1"

