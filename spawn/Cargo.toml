[dependencies]
color-eyre = {workspace = true}
parking_lot = {workspace = true}
pin-project-lite = {workspace = true}
tokio = { features = ["macros", "rt", "signal", "sync", "time", "tracing"], workspace = true }
tracing = {workspace = true}

[package]
edition = "2024"
name = "spawn"
version = "0.1.0"


[lints]
workspace = true
